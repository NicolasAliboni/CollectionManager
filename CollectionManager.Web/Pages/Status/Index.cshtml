@page
@model CollectionManager.Web.Pages.Status.IndexModel

@{
    ViewData["Title"] = "Status";
}

<div class="page-header d-flex justify-content-between align-items-center">
    <div>
        <h1>Status</h1>
        <p class="page-subtitle">
            Gerencie os status disponíveis para cada tipo de coleção.
        </p>
    </div>

    <a asp-page="Create" class="btn btn-primary">
        + Adicionar Status
    </a>
</div>

<div class="table-card">
    <div class="table-responsive">
        <table class="table collection-table align-middle">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Tipo</th>
                    <th class="text-end">Ações</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var status in Model.Status)
                {
                    <tr>
                        <td>
                            <strong>@status.Nome</strong>
                        </td>

                        <td>
                            <span class="item-badge">
                                @status.Tipo
                            </span>
                        </td>

                        <td class="text-end">
                            <a asp-page="Edit"
                               asp-route-id="@status.Id"
                               class="btn btn-sm btn-outline-secondary">
                                Editar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>