@page
@model CollectionManager.Web.Pages.Plataformas.IndexModel

@{
    ViewData["Title"] = "Plataformas";
}

<div class="page-header d-flex justify-content-between align-items-center">
    <div>
        <h1>Plataformas</h1>
        <p class="page-subtitle">
            Gerencie as plataformas utilizadas nos jogos e controles.
        </p>
    </div>

    <a asp-page="Create" class="btn btn-primary">
        + Adicionar Plataforma
    </a>
</div>

<div class="table-card">
    <div class="table-responsive">
        <table class="table collection-table align-middle">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th class="text-end">Ações</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var plataforma in Model.Plataformas)
                {
                    <tr>
                        <td>
                            <strong>@plataforma.Nome</strong>
                        </td>

                        <td class="text-end">
                            <a asp-page="Edit"
                               asp-route-id="@plataforma.Id"
                               class="btn btn-sm btn-outline-secondary">
                                Editar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>